<img class='banner' src="
                        {% if (service.banner) %}
                        {{asset(service.banner.slugBanner)}}
                        {% else %}
                        {{asset('img/category/default.png')}}
                        {% endif %}
                        ">