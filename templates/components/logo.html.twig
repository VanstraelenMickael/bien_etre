<img class='square_logo' src="
                        {% if (prestataire.logo) %}
                        {{asset(prestataire.logo.slug)}}
                        {% else %}
                        {{asset('img/users/default.png')}}
                        {% endif %}
                        ">